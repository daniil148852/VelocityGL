<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- OpenGL ES 3.2 requirement -->
    <uses-feature
        android:glEsVersion="0x00030002"
        android:required="true" />

    <application
        android:name=".VelocityGLApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VelocityGL"
        android:extractNativeLibs="true"
        tools:targetApi="34">

        <!-- Main Settings Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.VelocityGL">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Settings Activity -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Renderer Plugin Provider for Launchers -->
        <provider
            android:name=".plugin.RendererProvider"
            android:authorities="${applicationId}.renderer"
            android:exported="true"
            android:grantUriPermissions="true">
            <meta-data
                android:name="renderer_name"
                android:value="VelocityGL" />
            <meta-data
                android:name="renderer_version"
                android:value="1.0.0" />
            <meta-data
                android:name="gl_version"
                android:value="4.5" />
            <meta-data
                android:name="renderer_library"
                android:value="libvelocitygl.so" />
        </provider>

        <!-- Service for background operations -->
        <service
            android:name=".service.ShaderCompileService"
            android:exported="false" />

        <!-- Receiver for launcher integration -->
        <receiver
            android:name=".plugin.RendererReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="net.kdt.pojavlaunch.RENDERER_QUERY" />
                <action android:name="com.movtery.zalern.RENDERER_QUERY" />
                <action android:name="com.velocitygl.RENDERER_QUERY" />
            </intent-filter>
        </receiver>

        <!-- File Provider for sharing configs -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
